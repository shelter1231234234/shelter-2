import 'package:flutter/material.dart';

// ─────────────────────────────────────────────
// COLORS
// ─────────────────────────────────────────────
const kPrimary        = Color(0xFF3D5AF1);
const kPrimaryDark    = Color(0xFF2E46D6);
const kCardBlueStart  = Color(0xFF4B6CF7);
const kCardBlueEnd    = Color(0xFF3248C8);
const kGreenAccent    = Color(0xFF00C897);
const kGreenLight     = Color(0xFFE8FAF5);
const kRedAccent      = Color(0xFFFF6B6B);
const kRedLight       = Color(0xFFFFEEEE);
const kBackground     = Color(0xFFF5F6FA);
const kWhite          = Color(0xFFFFFFFF);
const kTextDark       = Color(0xFF1A1A2E);
const kTextMid        = Color(0xFF4A4A6A);
const kTextGrey       = Color(0xFF9A9AB0);
const kDivider        = Color(0xFFF0F1F7);
const kCardShadow     = Color(0x1A3D5AF1);

// ─────────────────────────────────────────────
// TEXT STYLES
// ─────────────────────────────────────────────
const kStyleHeading = TextStyle(
  fontSize: 20,
  fontWeight: FontWeight.w700,
  color: kTextDark,
  letterSpacing: -0.3,
);

const kStyleSubheading = TextStyle(
  fontSize: 17,
  fontWeight: FontWeight.w700,
  color: kTextDark,
  letterSpacing: -0.2,
);

const kStyleBody = TextStyle(
  fontSize: 14,
  fontWeight: FontWeight.w400,
  color: kTextMid,
);

const kStyleCaption = TextStyle(
  fontSize: 12,
  fontWeight: FontWeight.w400,
  color: kTextGrey,
);

// ─────────────────────────────────────────────
// DIMENSIONS
// ─────────────────────────────────────────────
const kPaddingH       = 22.0;
const kCardRadius     = 20.0;
const kItemRadius     = 16.0;
const kSmallRadius    = 12.0;
